#!/bin/zsh

if (( $+commands[firefox] )) && false; then
    firefox "$@"
elif (( $+commands[chromium-browser] )); then
    chromium-browser "$@"
elif (( $+commands[chromium] )) ; then
    chromium "$@"
fi
