#!/bin/zsh

if (( $+commands[firefox-nightly] )); then
    firefox-nightly "$@"
elif (( $+commands[chromium-browser] )); then
    chromium-browser "$@"
elif (( $+commands[chromium] )) ; then
    chromium "$@"
fi
