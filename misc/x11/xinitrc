#!/bin/zsh
set -x

source /home/thiderman/.zshrc

keyboard-setup &
xset -b
wp &
xrdb ~/.Xresources &

if [[ $(hostname) == "dragonkin" ]]; then
    xset s off
    xset -dpms
fi

exec nm-applet &

# export XORG_WM="hlwm"
# exec herbstluftwm --locked

export XORG_WM="i3"
exec i3
